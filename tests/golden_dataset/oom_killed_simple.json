{
  "id": "golden-001",
  "description": "Simple OOMKilled alert — scale memory, then verify pod stability via the K8s API",
  "expected_complexity": "simple",
  "input": {
    "alertname": "OOMKilled",
    "status": "firing",
    "labels": {
      "region": "us-east-1",
      "env": "prod",
      "cluster_id": "k8s-prod-1",
      "namespace": "checkout",
      "service": "checkout-api",
      "pod": "checkout-api-7d9f8b-xkj2p"
    },
    "annotations": {
      "summary": "Container checkout-api was OOMKilled",
      "description": "Container has been OOMKilled 3 times in the last 10 minutes"
    }
  },
  "expected_severity": "critical",
  "acceptable_actions": [
    "Increase memory limit",
    "increase memory",
    "memory limit",
    "kubectl set resources",
    "resource limit"
  ],
  "auto_approve": true,
  "mock_healthy_after_action": false,
  "expected_status": "escalated",
  "expected_status_note": "OOM verification reads real pod stability from the K8s API, not the mock metrics flag. With no live checkout-api pods, the agent must escalate rather than claim resolution."
}
